services: 
    zabbix-server:
        build:
            context: ./zabbix-server/
        pull_policy: build
        environment:
            TZ: Asia/Tokyo
            LANG: C
            LANGUAGE: C
            LC_ALL: C
        restart: unless-stopped
