services: 
    zabbix-server:
        build:
            context: ./zabbix-server/
        environment:
            TZ: Asia/Tokyo
            LANG: C
            LANGUAGE: C
            LC_ALL: C
        restart: unless-stopped
        networks:
           - frontend_bridge

networks:
    frontend_bridge:
